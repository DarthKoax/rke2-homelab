---
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: nginx-virtualservice
  namespace: istio-ingress
spec:
  gateways:
  - BarqueGateway
  hosts:
  - "nginx.local.barque.io"
  http:
  - match:
    - uri:
        prefix: /
    route:
    - destination:
        host: nginx.default.svc.cluster.local # This matches your Service metadata.name
        port:
          number: 80